import React from 'react'

const Header = () => {
    return (
        <div className="ui menu" style={{marginTop: "10px", display: "flex", justifyContent: "center", alignItems: "center"}}>
            <h2>Contact App</h2>
        </div>
    )
}

export default Header
